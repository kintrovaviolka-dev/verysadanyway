{
  "name": "lekar-portal-rozcestnik",
  "version": "1.0.0",
  "description": "Lékařský studijní portál s integrovaným asistentem Gemini",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env server.js",
    "build": "cd clinical-learning-portal && npm install --legacy-peer-deps && npm run build && cd ../urgentni-prijem && npm install --legacy-peer-deps && npm run build"
  },
  "dependencies": {
    "@google/genai": "^2.12.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  }
}
